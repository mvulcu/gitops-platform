apiVersion: v1
kind: Service
metadata:
  name: lingualink-backend
  namespace: lingua-app
  labels:
    app: lingualink-backend
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: lingualink-backend
